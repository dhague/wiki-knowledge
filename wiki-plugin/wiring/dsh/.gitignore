# Generated by scripts/generate-dsh-bundle.py — never committed. The patch
# bakes in this checkout's absolute plugin root (customSkillDirs is resolved
# against the DSH boot cwd), so it is per-checkout, not a repo artifact.
cordis.patch.yml
