{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); case \"$f\" in *.ts) [ -f ./enchiridion-ts/node_modules/.bin/prettier ] && ./enchiridion-ts/node_modules/.bin/prettier --write \"$f\" >/dev/null 2>&1 ;; esac; true"
          }
        ]
      }
    ]
  }
}
