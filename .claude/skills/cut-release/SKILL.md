---
name: cut-release
description: Cut a new release of the wiki-knowledge plugin. Use when asked to release, bump the version, or cut a release.
metadata:
  internal: true
---

# Cut a release

The procedure is `docs/agents/cut-release.md` at the repo root — read it and
follow it. It is the single source of truth for releases, shared by every host.
